services:
  spl-docs:
    image: rilusmahmud/spl-docs:main-1.0.0
    container_name: spl-maps
    restart: unless-stopped
    ports:
      - "4003:3000"
    env_file:
      - .env
    environment:
      - API_SPEC_URL=${API_SPEC_URL:-https://na-maps.vng-solutions.com/spl/openapi.json}
